<!--component/asynSwitch/asynSwitch.wxml-->
<view class="switch switch--{{ checked ? 'on' : 'off' }} {{ disabled ? 'swtich--disabled' : '' }}" bindtap="_handleSwitchChange">
    <view class="switch__circle">
        <view hidden="{{ !loading }}" class="switch__loading"></view>
    </view>
    <view class="switch__bg"></view>
</view>